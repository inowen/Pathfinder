/**
 * A Placement refers to the position (x,y) on the map and the orientation.
 */

class Placement {
    constructor(x, y, orientation) {
        this.x = x;
        this.y = y;
        this.orientation = orientation;
    }
}