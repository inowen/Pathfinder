
class Algorithms {

    // Add algorithms.
    // Field names ought to be: astar, dfs, bfs, it_deepening, greedy
    constructor() {
        // Test if I can create a function outside and assign this.field = function;
    }
}